# ForeScene Previs Schemas

Place reusable manifest, run-record, and review schemas for the canonical
ForeScene previs skill here. Harness adapters receive this directory through
`npm run sync:agent-skills` and must not maintain separate schema copies.
