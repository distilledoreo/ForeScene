/**
 * Camera template catalog for previs docs / skill references.
 */
export {
  FRAMING_COVERAGE,
  LENS_CLASS_FOCAL_MM,
  defaultLensClassForTemplate,
  verticalFovForLens,
  aimHeightFraction,
} from './framing';

export { PREVIS_CAMERA_TEMPLATES, type PrevisCameraTemplate } from './manifest';
