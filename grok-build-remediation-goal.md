# Grok Build remediation goal

Work directly in this ForeScene repository and implement the benchmark remediation completely. This is an implementation task, not a report-only task.

Read `AGENTS.md`, `docs/benchmark-luna-reasoning-effort-2026-08-12.md`, `docs/benchmark-token-usage-pricing-time-2026-08-12.csv`, `docs/prepared-media-audit-2026-08-07.md`, `docs/agent-api.md`, and the benchmark run reports under `C:\Users\disti\Documents\ForeScene Benchmark\music-video-v2-panorama-triad\runs`.

Resolve the current issues exposed by the Luna and Grok 4.6 benchmark runs across the app, API, CLI, export/package path, recovery resources, caching, cancellation/resume, visual preflight/quality gates, asset/pose contracts, and benchmark observability. Prioritize correctness first: one authoritative project revision; durable shot panorama unlinking; resource reconciliation; deterministic export by artifact/revision; no browser-download-event dependency; reliable profile forwarding and session refresh; consistent shot/object API scope and typed errors; reliable cache telemetry; real render job progress/cancel/resume semantics; and prevention of stale/reverted exports. Then address throughput: render-session/DAG reuse, avoiding repeated per-frame waits, bounded parallelism, streaming or memory-safe package assembly, and avoiding unnecessary Blob copies.

Add or update meaningful tests and documentation. Add visual preflight checks for subject visibility, framing/coverage, ground contact, camera direction, cropping, and motion continuity where the existing architecture permits. Make repair candidates transactional or preserve the best validated candidate so a later repair cannot silently regress a better result. Make asset/pose resolution and package inclusion explicit and machine-readable. Emit stable per-run provenance and telemetry: app/CLI/build identity, profile, project revision, cache decisions, retries, cancellation state, operation timings, artifacts, and validator/preflight results. Keep benchmark outputs and user-owned untracked `output/` artifacts untouched.

Use the smallest complete implementation, preserve unrelated work, and do not edit generated `.grok`, `.claude`, or `.kilo` adapters directly. Run targeted tests during implementation, then the relevant lint, build, unit/browser tests, and runtime smoke checks. Do not stop at identifying issues: make the changes, verify them, and report changed files, tests, runtime evidence, and any residual blocker.
